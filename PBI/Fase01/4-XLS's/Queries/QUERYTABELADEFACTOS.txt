SELECT 
t.TIME_ID,
c.CUSTOMERNUMBER,
o.ORDERNUMBER,
od.QUANTITYORDERED
FROM CUSTOMERS AS c
INNER JOIN ORDERS AS o
ON c.CUSTOMERNUMBER = o.CUSTOMERNUMBER
INNER JOIN DIM_TIME AS t
ON TO_TIMESTAMP(t.TIME_ID,'YYYY-MM-DD')= o.ORDERDATE
INNER JOIN ORDERDETAILS as od
ON o.ORDERNUMBER = od.ORDERNUMBER
